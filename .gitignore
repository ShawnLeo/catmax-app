.DS_Store
node_modules/
out/
dist/
*.log
.env
.env.local
.idea/
.eslintcache
# build artifacts
coverage/
.vite/
*.tsbuildinfo
# 本地图标/资源备份（如 icon 修复时的 .icon-backup-* 临时副本）
build/.icon-backup-*/
resources/.icon-backup-*/
# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

.zcode/
.agent/
.claude/
.agents/
cc-switch/
logs/
.pnpm-store/


# shadcn-vue
components.json-cache

# Auto-generated by unplugin-auto-import / unplugin-vue-components
src/renderer/src/auto-imports.d.ts
src/renderer/src/components.d.ts

# Hot Update: 发布产物与本地 manifest。
# 热更新包每次都不一样、体积几 MB，没有进版本库的理由；manifest 的权威副本在 R2 上。
release/
# 签名私钥绝不能进仓库（默认在 ~/.catmax/ 之外，这里是兜底：
# 万一有人用 --key 把它生成到了项目目录里）。
#
# 只匹配密钥文件本身，不要用 `*signing-key*` 这种宽泛写法——它会连
# scripts/generate-signing-key.cjs 一起挡掉，而那个脚本必须进仓库（它生成密钥，
# 自己不含密钥）。被 .gitignore 静默排除的源文件很难发现：本地能跑，别人 clone
# 下来却少一个文件。
*.pem
*signing-key*.key
hot-update-signing-key*
